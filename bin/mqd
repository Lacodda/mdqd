#!/usr/bin/env node

const yargs = require('yargs');
const mqd = require('../lib/mqd');

const parsed = yargs.argv;
const cmd = parsed._.shift();

(async () => {
  try {
    await mqd.load();
    await mqd.cli[cmd].apply(null, parsed._);
  } catch (error) {
    console.error(error);
  }
})();
